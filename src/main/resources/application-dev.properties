spring.profiles.include=log

spring.data.mongodb.database=${MONGO_INITDB_DATABASE}
spring.data.mongodb.host=${DB_HOST}
spring.data.mongodb.port=${DB_PORT}
spring.data.mongodb.username=${MONGO_INITDB_ROOT_USERNAME}
spring.data.mongodb.password=${MONGO_INITDB_ROOT_PASSWORD}
spring.data.mongodb.authentication-database=${DB_AUTH_MAN}

server.port=${PORT_API}
